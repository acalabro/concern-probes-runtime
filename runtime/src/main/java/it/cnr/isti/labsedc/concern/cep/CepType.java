package it.cnr.isti.labsedc.concern.cep;

/**
 * Wire-compatible copy of the monitor-side CepType.
 * Values and ordinal positions MUST stay identical to the original.
 */
public enum CepType {
    DROOLS, ESPER
}
